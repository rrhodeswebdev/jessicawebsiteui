version: 1
frontend:
    phases:
        preBuild:
            commands:
                - yarn install
                - nvm install 12
        build:
            commands:
                - yarn run build
                - nvm install
    artifacts:
        baseDirectory: .next
        files:
            - '**/*'
    cache:
        paths:
            - node_modules/**/*
